# tmper 当前状态

> 本文件只记录当前能力、已知限制和近期计划。用户说明见 `README.md`，实现架构见 `DESIGN.md`；`progress/` 是历史记录，不作为当前事实来源。

## 当前能力

- Linux 终端中的本地音乐播放，支持 Symphonia 提供的常见音频格式。
- 约两秒高水位的有界流式解码；切歌和 seek 会取消旧会话。
- Rodio 音频输出、暂停、音量、顺序/随机/单曲循环和真实 seek。
- LRC/增强 LRC 歌词、实时 FFT 频谱、内嵌封面与多级终端图像回退。
- 从文件浏览器添加**目录**到曲库并在后台增量扫描；CLI 的 `tmper play` 只接受**单个文件**。
- SQLite 曲库索引与 FTS5 前缀全文搜索，覆盖标题、艺术家、专辑和流派。
- M3U 导入/导出、七个 TUI 视图、主题与自定义快捷键。
- XDG 配置/数据/状态目录；默认配置和五套主题编译进可执行文件。

## 已知限制

- 只正式支持 Linux；macOS 和 Windows 未验证。
- CLI 尚不能把目录直接作为临时播放队列，目录导入需在文件浏览器中操作。
- 原生 Kitty/SIXEL 图像位于 Ratatui 缓冲区之外，终端兼容性取决于协议支持；通用回退是半块字符封面。
- 没有 MPRIS2、均衡器、在线歌词和桌面通知。
- 音频输出测试需要真实声卡或虚拟 ALSA，默认测试集合会跳过它们。

## 近期计划

1. 在更多 PipeWire/ALSA 环境做实际播放与快速切歌压力测试。
2. 评估 CLI 目录播放和曲库批量管理体验。
3. 在保持播放链路稳定的前提下继续拆分应用状态与事件处理。

## 文档职责

- `README.md`：安装、配置和使用说明。
- `DESIGN.md`：当前真实架构与实现约束。
- `STATUS.md`：当前能力、限制和近期计划。
- `progress/`：按日期保存的历史开发记录，可能包含已过时设计。
