# Phase 2 — 播放列表 + 元数据

**日期**: 2026-07-12
**状态**: ✅ 完成
**Commit**: `d4a34b2`

---

## Session 2.1 — 元数据读取（lofty 集成）

### 新建/修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/metadata/reader.rs` | 重写 | TrackInfo + read_metadata() |
| `tests/fixtures/test.flac` | 新建 | 带标签 FLAC |
| `tests/fixtures/test_notags.wav` | 新建 | 无标签 WAV |

### 关键实现
- `TrackInfo` — path, title, artist, album, album_artist, track, genre, year, duration, bitrate, sample_rate, channels, codec
- `read_metadata()` — `lofty::Probe` 读取标签
- Fallback: title → 文件名, artist → None
- `lofty::tag::ItemKey::AlbumArtist` 读取专辑艺术家
- codec 从文件扩展名推断

### 验收
- `cargo test`: 3/3 通过 (FLAC标签 + WAV无标签 + 不存在文件)
- `cargo clippy`: ✅ 零 warning

---

## Session 2.2 — 播放列表数据结构

### 新建/修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/playlist.rs` | 重写 | 完整实现 |
| `Cargo.toml` | 修改 | +rand = "0.8" |

### 关键实现
- `TrackEntry` — path, title, artist, duration_secs
- `Playlist` — tracks, current_index, name
  - `push()` / `insert_after_current()` / `remove()` — 自动修正 current_index
  - `next()` / `prev()` — 边界安全
  - `shuffle()` — Fisher-Yates，保持当前曲目
  - `sort_by(SortKey)` — Title/Artist/Album/Duration
  - `titles()` — Vec<&str>
- `SortKey` 枚举

### 验收
- `cargo test`: 7/7 通过（空列表、单曲、多曲、越界、删除修正索引、shuffle保持当前）
- `cargo clippy`: ✅ 零 warning

---

## Session 2.3 — 目录扫描器

### 新建/修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/library/scanner.rs` | 重写 | walkdir 扫描 |
| `Cargo.toml` | 修改 | +walkdir = "2" |

### 关键实现
- `scan_directory(dir, extensions)` — 递归遍历 + 扩展名过滤（大小写不敏感）
- 跟随符号链接
- 返回 `Vec<PathBuf>`

### 验收
- `cargo test`: 3/3 通过（扩展名过滤、空目录、不存在目录）
- `cargo clippy`: ✅ 零 warning

---

## Session 2.4 — 完整 Player View UI

### 新建/修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/ui/mod.rs` | 重写 | 4面板布局 |
| `src/app.rs` | 修改 | 集成 metadata + playlist |

### 关键实现
- 4 面板布局：顶部信息栏 → 进度条 → 播放列表 → 底部状态栏
- `TrackDisplay` — UI 曲目数据（path + title + artist + duration + is_playing）
- `RepeatMode` — Off/Track/Playlist + emoji 图标
- `load_and_play()` — read_metadata → 入列表 → 播放
- 播放列表 List widget：▶ 当前曲目 + 高亮选中项
- 总时长统计

### 验收
- `cargo build`: ✅ 零 warning
- `cargo test`: ✅ 18/18 通过
- `cargo clippy`: ✅ 零 warning

---

## Session 2.5 — Vim 风格键盘导航

### 新建/修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/input/handler.rs` | 重写 | KeyHandler 双键序列 |
| `src/event.rs` | 修改 | +JumpTop/JumpBottom/RemoveSelected |
| `src/app.rs` | 修改 | 完整键盘处理 |

### 关键实现
- **KeyHandler** — 200ms 窗口双键序列检测
  - `gg` → JumpTop
  - `dd` → RemoveSelected（自动修正 playing_index）
  - 超时/无效 → 传递原始按键
- **导航**: j/k/↓/↑（自动滚动）, G, Ctrl+d/u, gg
- **操作**: Enter 播放, Space 暂停, n/p 切歌, r/R 模式切换
- `move_selection()` + `move_scroll()` — 智能滚动

### 验收
- `cargo build`: ✅ 零 warning
- `cargo test`: ✅ 18/18 通过
- `cargo clippy`: ✅ 零 warning

---

## Session 2.6 — 搜索 + 完整配置 + 播放模式

### 新建/修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/config.rs` | 重写 | 5段完整配置 |
| `src/ui/mod.rs` | 修改 | 搜索过滤渲染 |
| `src/app.rs` | 修改 | 搜索模式 + 切歌逻辑 |

### 关键实现
- **完整 Config** (5 section): LibraryConfig, PlaybackConfig, VisualizerConfig, LyricsConfig, UiConfig
  - 每个 section 有独立的 Default impl 和 serde default 函数
  - XDG 路径加载 + toml 解析 + 硬编码回退
- **搜索**: `/` 进入模式 → 字符累积 → 实时过滤（title/artist 大小写不敏感）
  - Backspace 删除, Esc 清除
- **播放模式**: `on_track_ended()` — Track 循环 / Playlist 循环 / Off 顺序或停止

### 验收
- `cargo build`: ✅ 零 warning
- `cargo test`: ✅ 18/18 通过
- `cargo clippy`: ✅ 零 warning
