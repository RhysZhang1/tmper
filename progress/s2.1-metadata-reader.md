# Session 2.1 — 元数据读取（lofty 集成）

**日期**: 2026-07-12
**状态**: ✅ 完成
**Commit**: `d4a34b2`

## 修改文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/metadata/reader.rs` | 重写 | TrackInfo + read_metadata() |
| `tests/fixtures/test.flac` | 新建 | 带标签的 FLAC 测试文件 |
| `tests/fixtures/test_notags.wav` | 新建 | 无标签的 WAV 测试文件 |

## 关键实现
- `TrackInfo` — 完整字段：path, title, artist, album, album_artist, track, genre, year, duration, bitrate, sample_rate, channels, codec
- `read_metadata(path)` — 使用 `lofty::Probe` 读取标签，自动回退：
  - title 为空 → 使用文件名
  - artist 为空 → None（后续 UI 层回退 "Unknown Artist"）
- 使用 `lofty::tag::ItemKey::AlbumArtist` 读取专辑艺术家
- codec 从文件扩展名推断

## 验收
- `cargo test`: 3/3 通过
- `cargo clippy`: 零 warning
