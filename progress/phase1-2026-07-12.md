# Phase 1 — 核心播放 MVP

**日期**: 2026-07-12
**状态**: ✅ 完成
**Commit**: `ce8dd4b`

---

## Session 1.1 — Symphonia 音频解码器

### 新建/修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/audio/decoder.rs` | 重写 | AudioDecoder 实现 |
| `tests/fixtures/test.wav` | 新建 | 440Hz 测试音频 (ffmpeg) |

### 关键实现
- `AudioDecoder::open(path)` — `symphonia::default::get_probe()` 自动探测容器/编码
- `AudioDecoder::read_packet()` — 解码为 f32 PCM，`SampleBuffer<f32>` 统一转换
- `AudioDecoder::duration_secs()` — 总时长
- 处理 `DecodeError` / `IoError` / end-of-stream

### 验收
- `cargo test`: 2/2 通过 (decode_wav + nonexistent_file)
- `cargo clippy`: ✅ 零 warning

---

## Session 1.2 — Rodio 音频输出

### 修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/audio/output.rs` | 重写 | AudioOutput 封装 |

### 关键实现
- `AudioOutput::new()` — `OutputStream::try_default()` + `Sink::try_new()`
- `play_raw(samples, sample_rate, channels)` — `SamplesBuffer` 送入 Sink
- `pause()` / `play()` / `stop()` / `set_volume()` / `is_paused()` / `empty()`
- `_stream` 字段保持 OutputStream 存活

### 验收
- 编译通过，无单独测试（S1.3 集成测试）
- `cargo clippy`: ✅ 零 warning

---

## Session 1.3 — AudioEngine

### 修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/audio/engine.rs` | 重写 | 播放引擎 |

### 关键实现
- `play_file(path)` — 全量解码所有 packet → feed 到 Sink
- `Arc<Mutex<u64>>` 线程安全位置追踪
- `pause()` / `resume()` / `stop()` / `set_volume()`
- `position_secs()` / `duration_secs()` / `is_playing()`

### 验收
- `cargo test`: 3/3 通过 (lifecycle + position + stop_clears)
- `cargo clippy`: ✅ 零 warning

---

## Session 1.4 — TUI 骨架 + 事件循环 + MVP

### 新建/修改
| 文件 | 操作 | 说明 |
|------|------|------|
| `src/config.rs` | 重写 | XDG 路径 + toml 加载 |
| `src/cli.rs` | 重写 | clap derive (play 子命令) |
| `src/event.rs` | 重写 | AppEvent 枚举 |
| `src/ui/mod.rs` | 重写 | ratatui 渲染（标题栏+进度条） |
| `src/input/handler.rs` | 重写 | 键盘处理 |
| `src/app.rs` | 重写 | 主事件循环 |
| `src/main.rs` | 重写 | tokio + tracing 入口 |
| `Cargo.toml` | 修改 | +futures-util, crossterm event-stream |

### 关键实现
- crossterm raw mode + alternate screen
- `tokio::select!` 事件循环：键盘事件 + 100ms tick
- Space 暂停/恢复，q 退出，- = 调节音量
- 实时进度条 + 播放位置显示
- `cargo run -- play <file>` 播放音频

### 验收
- `cargo build`: ✅ 零 warning
- `cargo test`: ✅ 5/5 通过
- `cargo clippy`: ✅ 零 warning
- `cargo run`: ✅ 启动正常（需真实 TTY）
