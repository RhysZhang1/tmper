# tmper 默认配置模板。
# 首次运行时自动复制为 config/config.toml，之后修改 config/config.toml 生效。

[playback]
default_volume = 0.8
seek_step_small_secs = 5

[visualizer]
num_bars = 32
frame_rate = 30
smoothing = 0.35

[ui]
theme = "tokyo-night"
show_cover_art = true
